set timeout=0
set default=0

menuentry "robu_kernel" {
    insmod all_video
    insmod vbe
    insmod gfxterm
    set gfxpayload=keep
    terminal_output gfxterm
    multiboot2 /boot/robu_kernel @QEMU_APPEND@
    module2 /boot/rootfs.tar rootfs.tar
    boot
}
